const main = async () => {

const num = prompt("Digite o número inteiro: ")}
function calcularFatorial(numero, multiplicador){
// 5! 5*4*3*2*1 = 120
var fatorial = 1
//variavel i
for (var i = 1; i <= numero; i++) {
fatorial = fatorial * i
}
document.write('Fatorial de $(numero): $(fatorial)');
}

main()
